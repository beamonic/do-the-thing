# Plan against SPEC-17 revision 3

| Task | Description | Status |
|---|---|---|
| T-01 | Directory walk that collects `*.csv` paths | done — `summary.py` reads the directory |
| T-02 | Row parsing that skips empty or non-numeric `amount` (SPEC-17-AC-03) | done |
| T-03 | JSON output with `total` and `count` (SPEC-17-AC-02) | **not started** |
| T-04 | Empty-directory behavior (SPEC-17-AC-04) | **not started** |
| T-05 | Tests invoking the CLI as a subprocess | **not started** |
